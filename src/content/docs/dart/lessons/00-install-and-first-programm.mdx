---
title: 0. Первая программа
description: Как установить Dart и написать первую программу. main(), print().
---

В этом уроке мы установим программу в которой будем писать код (VSCode), язык программирования Dart и напишем на нем нашу первую программу.

## Что делают програмисты?

Кроме всего прочего они **пишут код**.
Код это простой текст.
Значит чтобы писать его нам нужна программа для редактирования текста, подойдет любая, даже встроенный Блокнот.

Но есть специальные программы, которые делают процесс написания кода проще и удобнее.
Самая популярная из них — **Visual Studio Code (VS Code)**.
Давайте установим её.

1. Зайди на https://code.visualstudio.com  
2. Скачай и установи VS Code  
3. Открой VS Code

### Гайд на VS Code
{/*
Про то что это такое и зачем нужно,
Про разные окна в нем и про все то чем будем пользоваться
*/}
...

## Установка Dart

1. В VS Code открой **Extensions** (Ctrl + Shift + X)
2. Найди **Flutter** (автор: Dart Code)
3. Нажми **Install**

Этот плагин установит Dart, Flutter (для создания приложений с интерфейсом) и сам всё настроит.

Если VS Code спросит:
> *Install Flutter SDK?*  
— соглашайся.

#### Проверим, что всё работает

Открой терминал прямо в VS Code:  
Terminal → New Terminal

Напиши в терминале:

```bash
dart --version
```

Если появилась версия — всё готово ✅

---

Теперь нам нужно обедиться что все работает. Давай напишем для этого нашу первую программу.

## Первая программа

В терминале VS Code напиши:

```bash
code hello.dart
```

Что это значит:

* `code` — команда открыть файл в VS Code
* `hello.dart` — имя файла
* если файла нет, он **создастся автоматически**

В открывшемся файле `hello.dart` напиши:

```dart
void main() {
  print('Привет, Dart!');
}
```

Коротко:

* `main()` — с этого места программа начинает работу
* `print()` — выводит текст на экран
* `;` — конец команды

В терминале напиши:

```bash
dart hello.dart
```

Если всё ок, ты увидишь:

```text
Привет, Dart!
```

🎉 Поздравляю! Ты только что написал свою первую программу на Dart.

---
## Что дальше?

Дальше разберём:

* что такое команды
* как программа запоминает данные
* как заставить её делать разные вещи

Поехали дальше 🚀
