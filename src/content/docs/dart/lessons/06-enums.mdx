---
title: 6. Перечисления (enums)
description: enums
---

Вспомним: в программировании у нас есть **типы данных**
:::note
Типы с которыми мы уже встречались: `int`, `String`, `double`, `bool`, `List`, `Set`, `Map`.
:::
Каждый тип хорошо выражает *что-то своё*.  
Например `int` подходит для числовых значений например возраст (количество лет), а `String` для строковых, например для имени:

```dart
int age = 16;          // Возраст
String name = "Игорь"; // Имя
```

Но так происходит не со всем.

В частности как выразить в коде “**результат игры**” в Камень-Ножницы-Бумага?

> У нас есть три возможных варианта: *победил*, *проиграл*, *ничья*.

Как сохранить эти значения в переменной, передавать по программе и делать разные действия в зависимости от результата?

### Когда мы пытаемся выкрутиться числами

Можно “договориться”, что:

* `-1` = проиграл
* `0` = ничья
* `1` = победил

```dart
// Функция которая считает результат раунда
int computeRoundResult(/* ... */) {
  return 1; // Возвращаем победу
}
```

Вроде бы работает. Но это работает “на доверии”.

Потому что число **само по себе ничего не говорит**.

Вот ты видишь:

```dart
if (result == 1) { /* ... */ }
```

И мозг такой: “Окей… а 1 это что? победа? ничья? кто придумал?”

А теперь представь, что через неделю ты забудешь договорённость.
Или другой человек откроет твой код.
Или ты сам вернёшься через месяц.

И ещё хуже: `int` разрешает любые числа.

```dart
int result = 7;   // 🤷‍♂️ это что вообще?
int result = -42; // тоже "валидно" для int
```

Компилятор не сможет тебе сказать: “эй, ты хранишь бред”. Он не покажет ошибку, не в рекдакторе кода (красным), не во время работы программы.

---

### Тогда давайте строками

Окей, давай прямо текстом:

> `"победил"`, `"проиграл"`, `"ничья"`

```dart
// Функция которая считает результат раунда
String computeRoundResult(/* ... */) {
  return "победил"; // Возвращаем победу
}
```

Строка понятнее числа: “победил” видно глазами.

Но тут другая ловушка: строка — это **любая** последовательность символов.

И значит, можно случайно сделать так:

```dart
return "победил "; // пробел в конце
return "Победил";  // другая буква
return "победел";  // опечатка
return "win";      // кто-то решил по-английски
```

И программа это *примет*. Она не умеет читать смысл, она видит просто буквы.

Самое неприятное: такие ошибки часто находят себя не сразу, а “где-то потом”, когда логика внезапно не совпала. Это означает что искать эту ошибку будет очень долго и сложно!

---

### Что мы на самом деле хотим

Мы не хотим “число” и не хотим “просто текст”.

Мы хотим вот такую штуку:

> Переменная **может быть только одним из трёх вариантов**.
> И **других значений не существует**.

То есть нам нужен свой **тип**, который хранит *ограниченный набор значений* которые за что то отвечают.

Для этого в Dart есть `enum` — *перечисление*.

---

## Enum - это ограниченный список вариантов

Создаём свой тип “РезультатРаунда”:

```dart
// Используем ключевое слово "enum" и затем название с большой буквы
enum RoundResult { win, lose, draw }
```

С этого момента:

* `RoundResult` — это новый тип
* а `win`, `lose`, `draw` — единственные допустимые значения

Теперь перепишем функцию:

```dart
RoundResult computeRoundResult(/* ... */) {
  return RoundResult.win;
}
```
И все, теперь эту функция сможет вернуть только те значения которые нам нужны

---

### Делать разные действия - удобно и надежно

Самая частая работа с enum: “в зависимости от значения — делай разное”.

```dart
enum RoundResult { win, lose, draw }

void main() {
  var result = RoundResult.draw;

  // Как if-else только с применениями switch (с англ.: переключение) выражения:
  switch (result) {
    case RoundResult.win: // case == случай
      print("Ты победил!");
    case RoundResult.lose:
      print("Ты проиграл.");
    case RoundResult.draw:
      print("Ничья.");
  }
  // `switch` здесь как табличка “если это — то делай вот это”.
}
```

И приятный бонус: если ты добавишь новый вариант, например:

```dart
enum RoundResult { win, lose, draw, cancel }
```

то места со `switch` начнут “ругаться” и просить:
“а что делать с `cancel`?”

То есть enum помогает не забывать логику в разных уголках программы. И скажет тебе поменять код, когда в нем что то поломалось из-за изменений.
:::caution
if-else не напомнит тебе поменять код когда ты добавишь что то к своему enum
```dart
  if (result == RoundResult.win) {
    print("Ты победил!");
  } else if (result == RoundResult.lose) {
    print("Ты проиграл.");
  } else {
    print("Ничья.");
  }
```
:::

### Иногда нужно превратить enum в текст

Enum — это “внутренние” значения для программы.
А человеку на экране нужен текст.

```dart
enum RoundResult {
  win("Ты победил!"),
  lose("Ты проиграл!"),
  draw("Ничья!");

  final String message;

  const RoundResult(this.message);
}

void main() {
  print(RoundResult.win.message); // Запринтит "Ты победил!"
}
```

---

## Мини-задача

Сделай enum для светофора, какие цвета у него есть?
А потом в зависимости от света светофора выводи, что делать: `Стой`, `Жди`, `Едь`